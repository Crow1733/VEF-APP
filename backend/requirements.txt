# Dependencias de runtime del backend VEF (Python 3.11+).
# Se usan versiones mínimas (>=) en vez de exactas para que pip pueda instalar
# aunque una versión concreta no esté disponible o la red sea lenta.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7

# Opcional: solo para importar el catálogo desde Excel con backend/import_excel.py.
# El servidor NO lo necesita para funcionar. Descoméntalo si vas a usar esa utilidad.
# openpyxl>=3.1
